# EPG Bulk Rename Folders

Open `EPG > Tools > Bulk Rename Folders`, select a parent folder, prefix, and starting index. Optionally rename texture assets inside each renamed folder. Commit or back up assets first.
