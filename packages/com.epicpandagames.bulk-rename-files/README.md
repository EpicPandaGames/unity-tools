# EPG Bulk Rename Files

Open `EPG > Tools > Bulk Rename Files`, select a project folder, prefix, and starting index. The tool renames files directly through `AssetDatabase`; commit or back up assets first.
