# EPG Replace With Prefab

Open `EPG > Tools > Replace With Prefab`. Assign a prefab and select scene objects to replace. Names, parents, sibling positions, and local transforms are retained, with Unity Undo support.
