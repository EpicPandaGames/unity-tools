# EPG Resize Pow2

Open `EPG > Tools > Resize Pow2`. The selected texture is padded according to the chosen alignment and overwritten as a transparent square power-of-two PNG. Commit or back up the texture first.
